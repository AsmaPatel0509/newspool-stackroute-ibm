import { AppPage } from './app.po';
import { browser, logging } from 'protractor';

describe('news-app-angular App', () => {
  let page: AppPage;

  beforeEach(() => {
    page = new AppPage();
  });

  

  // it('should display Menu button', () => {
  //   page.navigateTo();
  //   expect(page.getMenuButton()).toBeTruthy(`<button> should exist in header.component.html`);
  // }); 

  
});
